# Required: Microsoft Entra ID (Azure AD) application (client) ID
EMAIL_LINEAR_CLIENT_ID=

# Optional: Tenant ID (defaults to "common" if unset)
# EMAIL_LINEAR_TENANT_ID=

# Optional: Client secret (if configured for your app registration)
# EMAIL_LINEAR_CLIENT_SECRET=
